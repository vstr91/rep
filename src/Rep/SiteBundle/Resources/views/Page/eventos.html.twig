{% extends "RepSiteBundle:Page:interno.html.twig" %}
{% block conteudo %}
        <div class="row">
            <div class="col-md-12 bg-info">
                <p class="text-center">Bot&atilde;o Direito do mouse sobre evento no calend&aacute;rio: 
                    Op&ccedil;&otilde;es do Evento</p>
                <p class="text-center">Bot&atilde;o Esquerdo do mouse sobre data no calend&aacute;rio: 
                    Abre formul&aacute;rio de Cadastro de Evento</p>
            </div>
        </div>
        <div class="row">
            <div class="col-md-11">
{#                <h3 class="text-center">Agenda de Eventos</h3>#}
                <div id="calendar"></div>
            </div>
        </div>
                <div class="row">
                    <div class="col-md-10">
                        <h3>Legenda</h3>
                        {% for tipo in tiposEvento %}
                            <p style="color: {{ tipo.cor }}">{{ tipo.nome }}</p>
                        {% endfor %}
                    </div>
                </div>
        {#<div class="row">
            <div class="col-md-12">
                <h3 class="text-center">Eventos Cadastrados</h3>
                {% include 'RepSiteBundle:Evento:tabela-cadastrados.html.twig' 
                          with {'eventos': eventos} %}
            </div>
        </div>#}
    <div class="modal fade" id="modal-edicao">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header">
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
                    <h4 class="modal-title" id="titulo-modal">Editar Evento</h4>
                </div>
                <div class="modal-body" id="body-edicao">

                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-default" data-dismiss="modal">Fechar</button>
                    <button type="button" class="btn btn-primary" id="btn-editar">Salvar</button>
                </div>
            </div>
        </div>
    </div>
    <div class="modal fade" id="modal-cadastro">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header">
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
                    <h4 class="modal-title" id="titulo-modal">Cadastrar Evento</h4>
                </div>
                <div class="modal-body" id="body-edicao">
                    <form name="form-evento" id="form-evento" method="POST" 
                          action="{{ path('rep_site_eventos_cadastra', {'id_evento': -1}) }}">
                        {{ form_widget(formEvento) }}
                    </form>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-default" data-dismiss="modal">Fechar</button>
                    <button type="button" class="btn btn-primary" id="btn-cadastrar">Salvar</button>
                </div>
            </div>
        </div>
    </div>
{% endblock %}