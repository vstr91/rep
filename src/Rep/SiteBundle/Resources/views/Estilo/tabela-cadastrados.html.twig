<div class="table-responsive">
    <table class="table table-hover table-striped text-center">
        <thead>
            <tr>
                <td>Nome</td>
                <td>A&ccedil;&otilde;es</td>
            </tr>
        </thead>
        <tbody>
            {% for estilo in estilos %}
                <tr>
                    <td>{{ estilo.nome }}</td>
                    <td>
                        <a class="link-editar" href="#{{ estilo.id }}">Editar</a>
                    </td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
</div>