<form id="form-estilo-novo" name="form-estilo-novo" 
      action="{{ path('rep_site_estilos_cadastra_novo', {'id_estilo': estilo.id}) }}" 
      method="POST" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <input type="submit" class="hide" id="btn-submit-form" value="Cadastrar" />
    </p>
</form>
<script type="text/javascript">
    {#$('#form-parada').submit(function(){
        $(this).ajaxSubmit(function(data){
            console.log(data);
            $(".caixaForm").dialog('close');
            return false;
        });
        return false;
    });#}
    
    $(document).ready(function(){
        $('#btn-cadastrar-novo-estilo').click(function(){

    {#        $('#form-artista-novo').submit();#}

            $('#form-estilo-novo').submit(function(){
                
                $(this).ajaxSubmit({
                    type: "POST",
                    success: function(response){
                        alert("Estilo Cadastrado");
                        $('#modal-novo-estilo').modal('hide');
                    }
                });

                return false;

            });
            
            $('#btn-submit-form').click();
            
        });
    });
    
    
</script>