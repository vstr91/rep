<form id="form-musica-nova" 
      action="{{ path('rep_site_musicas_projetos_novas_cadastra', {'id_musica': musica.id, 'id_projeto': projeto.id}) }}" 
      method="POST" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
{#        <input type="submit" class="hide" id="btn-submit" value="Cadastrar" />#}
    </p>
</form>
<script type="text/javascript">
    {#$('#form-parada').submit(function(){
        $(this).ajaxSubmit(function(data){
            console.log(data);
            $(".caixaForm").dialog('close');
            return false;
        });
        return false;
    });#}
    
    $('#btn-cadastrar-nova').click(function(){
        $('#form-musica-nova').submit();
    });
</script>