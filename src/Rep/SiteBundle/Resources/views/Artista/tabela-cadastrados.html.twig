<table class="table table-hover table-striped text-center">
    <thead>
        <tr>
            <td>Nome</td>
            <td>A&ccedil;&otilde;es</td>
        </tr>
    </thead>
    <tbody>
        {% for artista in artistas %}
            <tr>
                <td>{{ artista.nome }}</td>
                <td>
                    <a class="link-editar" href="#{{ artista.id }}">Editar</a>
                </td>
            </tr>
        {% endfor %}
    </tbody>
</table>