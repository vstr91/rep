<form id="form-repertorio-edita" 
      action="{{ path('rep_site_repertorios_cadastra', {'id_repertorio': repertorio.id, 'id_projeto': projeto.id}) }}" 
      method="POST" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
{#        <input type="submit" class="hide" id="btn-submit" value="Cadastrar" />#}
    </p>
</form>
<script type="text/javascript">
    {#$('#form-parada').submit(function(){
        $(this).ajaxSubmit(function(data){
            console.log(data);
            $(".caixaForm").dialog('close');
            return false;
        });
        return false;
    });#}
    
    $('#btn-cadastrar-repertorio').click(function(){
        $('#form-repertorio-edita').submit();
    });
</script>