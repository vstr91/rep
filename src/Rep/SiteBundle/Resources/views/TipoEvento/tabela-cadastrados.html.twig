<table class="table table-hover table-striped text-center">
    <thead>
        <tr>
            <td>Nome</td>
            <td>A&ccedil;&otilde;es</td>
        </tr>
    </thead>
    <tbody>
        {% for tipoEvento in tiposEvento %}
            <tr>
                <td>{{ tipoEvento.nome }}</td>
                <td>
                    <a class="link-editar" href="#{{ tipoEvento.id }}">Editar</a>
                </td>
            </tr>
        {% endfor %}
    </tbody>
</table>